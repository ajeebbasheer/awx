- name: Ping and List
  hosts: "{{ variable_host | default('all') }}"
  tasks:
    - name: Ping
      ping:
#    - name: list
#      command: ls
#      register: myoutput
#    - set_fact:
#        myoutput={{ myoutput.stdout }}
#    - debug: var=myoutput
#      run_once: true
