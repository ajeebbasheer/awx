- name: adding host playbook
  hosts: localhost
  tasks:
  - name: Add host to group 'just_created' with variable foo=42
    add_host:
      name: 10.189.126.15
      groups: ayushservers_1
      foo: 42
- name: Ping and List
  hosts: ayushservers_1
  tasks:
    - name: Hello Message
      ping:
    - name: list
      command: ls
      register: myoutput
    - set_fact:
        myoutput={{ myoutput.stdout }}
    - debug: var=myoutput
      run_once: true
